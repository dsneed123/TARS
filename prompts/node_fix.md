The build/tests failed after an implementation pass. Fix the failure.

Task being implemented: {{TASK_TITLE}}

Failure output (tail):
```
{{ERROR_TAIL}}
```

Instructions: find the root cause, make the minimal fix, then re-run the failing
command with run_command to confirm it passes. Do not refactor unrelated code.
Call finish when the failure is resolved.
